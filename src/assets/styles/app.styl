@require "iconfont/iconfont.css"
@require "theme-blue.styl"

.iconfont
  position relative
  display inline-block
  font-size 14px
  transform scale(16 / 14)
  padding-left 1px
  padding-right 1px

  &.normal
    transform none